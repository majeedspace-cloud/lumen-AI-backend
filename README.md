# Backend folder

This folder is where the FastAPI app should live.

Use this structure:

```text
backend/
├── main.py
└── ...
```

If you want a cleaner setup later, you can split code into:
- routes/
- models/
- services/
- config/
